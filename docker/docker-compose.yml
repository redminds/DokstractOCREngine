services:
  ocr-engine:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    environment:
      ENGINE_MAX_INFLIGHT_REQUESTS: ${ENGINE_MAX_INFLIGHT_REQUESTS:-2}
    ports:
      - "127.0.0.1:${ENGINE_HOST_PORT:?ENGINE_HOST_PORT is required}:8010"
    volumes:
      - ../.data:/app/.data
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8010/health').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - dokstract-shared

networks:
  dokstract-shared:
    external: true
    name: dokstract-shared
